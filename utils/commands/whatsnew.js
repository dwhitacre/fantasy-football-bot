import Base from './_base'

export default class WhatsNew extends Base {
  id = 'whatsnew'
  desc = 'What is new in the latest update!'
  text = 'The bot is back! You can now use commands anywhere in your message!'
}
