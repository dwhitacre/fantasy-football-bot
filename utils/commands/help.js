import Base from './_base'

export default class Help extends Base {
  id = 'help'
  desc = 'Helpful information on using commands.'
  text = 'Type !list to see the available commands. Use the commands anywhere in your message, and I will respond!'
}
